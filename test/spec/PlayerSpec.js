describe("Translation", function() {
  var app = app || {};

  beforeEach(function() {
  });

  it("should spawn the app without problems", function() {
    app = app || {};
    console.log(app);
    new app.TranslationView();
  });
});